version: "3.8"
services:
  app:
    build: 
      dockerfile: Dockerfile
      context: .
    env_file:
      - .env
    ports:
      - "${PORT}:80"
    restart: always
    volumes:
      - .:/src/app